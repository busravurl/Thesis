import React from 'react';
import {SafeAreaView, Text} from 'react-native';

function Camera() {
    return(
        <SafeAreaView>
            <Text>Camera</Text>
        </SafeAreaView>
    );
};

export default Camera;